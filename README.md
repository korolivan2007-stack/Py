# Telegram Styler Bot

## 📌 Описание
Telegram Styler Bot — это Python-бот для Telegram, который предоставляет инструменты для стилизации текста с поддержкой Markdown, HTML и пользовательских стилей.

## ✨ Основные возможности
- **Стилизация текста**: жирный, курсив, подчеркнутый, зачеркнутый
- **Форматирование**: Markdown, HTML, моноширинный текст
- **Генерация ASCII-арта** и стилизованных заголовков
- **Конвертация текста** в различные стили (инверсный, верхний регистр, шаблонный)
- **Меню настроек** для персонализации стилей

## 🛠 Технологии
- `python-telegram-bot` — для работы с Telegram API
- `aiogram` — альтернативная библиотека (опционально)
- `requests` — для HTTP-запросов
- `python-dotenv` — для управления конфигурацией

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/yourusername/telegram-styler-bot.git
cd telegram-styler-bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации
Создайте файл `.env` в корневой директории:
```env
BOT_TOKEN=your_telegram_bot_token_here
ADMIN_ID=your_admin_user_id
```

### 4. Запуск бота
```bash
python bot.py
```

## 📁 Структура проекта
```
telegram-styler-bot/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация и настройки
├── handlers/           # Обработчики сообщений
│   ├── commands.py     # Обработчики команд
│   ├── text.py         # Обработчики текста
│   └── callbacks.py    # Обработчики callback-запросов
├── keyboards/          # Клавиатуры и inline-кнопки
├── utils/              # Вспомогательные функции
│   ├── styler.py       # Функции стилизации текста
│   └── formatters.py   # Форматирование текста
├── .env.example        # Пример файла конфигурации
├── requirements.txt    # Зависимости проекта
└── README.md           # Документация
```

## ⚙️ Команды бота
- `/start` — Приветственное сообщение и начало работы
- `/help` — Список команд и инструкции
- `/styles` — Показать доступные стили текста
- `/settings` — Настройки бота
- `/format` — Показать примеры форматирования

## 🔧 Кастомизация

### Добавление новых стилей
Редактируйте файл `utils/styler.py`:
```python
def custom_style(text):
    return f"✨ {text} ✨"
```

### Изменение клавиатур
Редактируйте файлы в директории `keyboards/` для настройки интерфейса.

## 📝 Пример использования
```
Пользователь: /styles
Бот: Выберите стиль текста:
     1. Жирный — **текст**
     2. Курсив — _текст_
     3. Код — `текст`
     4. ASCII — стилизованный текст
```

## 🤝 Вклад в проект
1. Форкните репозиторий
2. Создайте ветку для вашей функции
3. Внесите изменения
4. Отправьте Pull Request

## 📄 Лицензия
Проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.

## 📞 Поддержка
- Issues: [GitHub Issues](https://github.com/yourusername/telegram-styler-bot/issues)
- Контакты: ваш-email@example.com

---

⭐ Если вам нравится проект, поставьте звезду на GitHub!
